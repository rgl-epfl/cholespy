[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "scikit-build",
    "cmake>=3.17",
    "nanobind>=0.0.2",
    "ninja; platform_system!='Windows'"
]

build-backend = "setuptools.build_meta"
