[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "scikit-build==0.14.0",
    "cmake>=3.18",
    "nanobind>=1.0.0",
    "ninja; platform_system!='Windows'"
]

build-backend = "setuptools.build_meta"
